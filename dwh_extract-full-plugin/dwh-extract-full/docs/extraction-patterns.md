# Extraction patterns: full, incremental (watermark), CDC (log/trigger), file-arrival, streaming/bounded; parallel slicing; restartability

> SCAFFOLD ONLY - to be written. Placeholder so the structure is visible.
