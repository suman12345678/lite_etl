# Default local landing root. Configurable via config/landing_zone/layout.yaml or ETL_LANDING_ROOT. Layout: <domain>/<source>/<load_date=YYYY-MM-DD>/<run_id>/{data.*, _SUCCESS, manifest.json, _reconciliation.json, rejects.*}

> SCAFFOLD ONLY - to be written. Placeholder so the structure is visible.
