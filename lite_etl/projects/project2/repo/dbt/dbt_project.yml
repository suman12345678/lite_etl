name: project2
version: "1.0.0"
config-version: 2
profile: project2

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  project2:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  project2:
    control_totals:
      +column_types:
        expected_value: double

# Transform logic is engine-agnostic. Pick the warehouse in profiles/profiles.yml
# or via the Terraform `engine` variable in ../infra.
