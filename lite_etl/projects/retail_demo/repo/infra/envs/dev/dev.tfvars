# dev overrides. `engine` here is the whole "swap warehouse" lever:
#   engine = "duckdb"      # default, no cloud
#   engine = "databricks"  # fill profiles.yml + module TODOs
#   engine = "snowflake"   # fill profiles.yml + module TODOs
engine = "duckdb"
