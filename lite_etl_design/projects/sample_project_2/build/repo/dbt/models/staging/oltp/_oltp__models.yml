version: 2
models:
  - name: stg_oltp__customers
    columns:
      - name: customers_id      # TODO real key
        tests: [not_null, unique]
  - name: stg_oltp__products
    columns:
      - name: products_id      # TODO real key
        tests: [not_null, unique]
  - name: stg_oltp__orders
    columns:
      - name: orders_id      # TODO real key
        tests: [not_null, unique]
  - name: stg_oltp__order_lines
    columns:
      - name: order_lines_id      # TODO real key
        tests: [not_null, unique]
