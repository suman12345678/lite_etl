-- {{ config(tags=['recon']) }}
-- every refund links to an order; sum per customer/period within tolerance. requirements/05
-- TODO: return rows that VIOLATE the rule (0 rows = pass)
select 1 as _todo where false
