# shopify config - no secrets (secret-scope:// refs live in the pipeline env).
source: shopify
objects: []          # TODO tables / endpoints / file pattern from requirements/01
lookback_minutes: 60  # TODO per shape
page_size: 1000
timeout_s: 30
